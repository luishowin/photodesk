[package]
name = "photodesk-color"
description = "Spike B — colour validation harness. Permanent suite (ARCHITECTURE.md §2.2, §13)."
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
half.workspace = true
# The suite tests the product's colour code, not a copy of it. See src/lib.rs.
photodesk = { path = "../../src-tauri" }

[dev-dependencies]
lcms2.workspace = true
libheif-rs.workspace = true
