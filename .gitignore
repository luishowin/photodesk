# Rust
/target/
**/*.rs.bk
Cargo.lock.bak

# Front end
node_modules/
dist/

# Tauri writes these from tauri.conf.json and capabilities/ on every build
/app/gen/schemas/

# PhotoDesk caches — regenerable by definition, never committed
.photodesk/

# Packaging
*.rpm
/packaging/rpm/build/

# Editors and OS
.vscode/
.idea/
*.swp
.DS_Store

# Spike C generated artefacts — emitted by `cargo test -p photodesk-renderer-spike`,
# regenerable by definition, and two of them are 1 MB float dumps
/tests/renderer/web/generated/
